# Executable family source

A `CREATE_FAMILY` decision must provide `executable_family_source_ref`. Compilation succeeds only when the corresponding immutable family source exposes the same `source_ref`.
